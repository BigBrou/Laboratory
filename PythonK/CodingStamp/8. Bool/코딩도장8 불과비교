8. 불과 비교, 논리 연산자
  - 참(True), 거짓(False)를 나타내는 boolean

  - 같은 지 다른지 비교하기
    = 10 == 10
    = 10 != 10

  - 문자열은 대소문자 구분
    = 'Python' == 'python' (False)

  - 객체와 객체의 비교(is, is not)
    = 1 is 1.0
    = 1 is not 1.0
  - 정수 객체와 실수 객체의 차이점 구분
    = id() 를 이용, id는 객체의 고유한 값(메모리 주소)을 구하는 기능
  - 값을 비교할 때는 is를 사용해선 안된다.

  - 논리연산자
    = and, or, not
    = 항상 비교연산자 후, 논리연산자 진행 순서
      not 1 is 1.0 (not False)

  - 정수, 실수, 문자열 불 처리
    = 정수 0, 실수 0.0 이외의 모든 수는 True
      = bool(1)
      = bool(0)
      = bool(1.5)

    = 값이 있는 경우에는 True 처리
      = bool('False')   #[True]


  - 단락 평가 (short-circuit evaluation)
    = 첫 번째 값만 결과가 확실하면 두 번째 방법은 확인하지 않는 방법
    = 특이한 점은 python은 마지막에 실행한 평가값을 그대로 반환
      = True and 'python'
