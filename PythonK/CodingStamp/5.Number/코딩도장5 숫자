5. 숫자 계산하기
  - 숫자는 정수, 실수, 복소수 형태로 이루어져 있다. (int, float, complex)

  정수 계산하기
    - 기본사칙연산 [+, -, *, /]
    - 몫 [//], 나머지 [%], 거듭제곱 [**]
    - 행렬 곱셈 [@]
    - 정수에서 정수를 나누었을 때 나머지가 없으면 정수로, 나머지가 있으면 실수로 변환
    - 실수를 정수로 나타내고 싶으면 int() 로 Casting

    - divmod(,)
      = 몫과 나머지를 같이 구할 수 있는 함수

    - 진수표현
      =  2진수 - 숫자 앞에 0b를 붙임
      =  8진수 - 숫자 앞에 0o를 (숫자 0과 소문자 o)를 붙임
      = 16진수 - 숫자 앞에 0x or 0X를 붙이며, 0~9, A~F 사용
