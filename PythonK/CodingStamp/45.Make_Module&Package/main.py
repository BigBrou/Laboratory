#import calcpkg.geometry as calc_geo
#import calcpkg.operation as calc_oper

import calcpkg
from calcpkg import *

print(calcpkg.add(10, 20))
print(calcpkg.mul(10, 20))

print(triangle_area(10, 20))
print(rectangle_area(10, 20))
